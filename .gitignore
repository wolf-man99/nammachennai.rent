node_modules
.next
out
build
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Local data + uploads
.data
public/uploads

# Secrets. Every .env variant is ignored; only the placeholder template is tracked.
.env
.env.*
!.env.example
*.pem
*.key
